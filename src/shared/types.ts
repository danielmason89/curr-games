import type { Game, GameDetails, GamesResponse, PlatformDetails } from '@/server/lib/schemas.js';

// Re-export the types from the server to be used in the client
export type { Game, GameDetails, GamesResponse, PlatformDetails };

// Define the shared types for both client and server here:
